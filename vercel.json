{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/next" }
  ],
  "functions": {
    "api/**/*.{js,ts}": {
      "runtime": "nodejs20.x"
    },
    "app/**/route.{js,ts}": {
      "runtime": "nodejs20.x"
    }
  },
  "buildCommand": "pnpm prisma:generate && pnpm build"
}
