"use client";

import * as React from "react";
import { useRouter, useSearchParams } from "next/navigation";
// ここへ既存 /writer の画面実装（JSX / state / effect / handlers）を順次移植してください。
// いったんプレースホルダを返します。

export default function ClientPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Writer Client Page</h1>
      <p className="text-sm text-muted-foreground mt-2">
        プレースホルダ：このファイルに /writer のクライアント処理を移植します。
      </p>
    </div>
  );
}
